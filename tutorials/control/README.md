# Control Tutorial

Run:

```bash
uv run python tutorials/control/control_pick_place.py
```

Notes:
- Uses `inverse_kinematics` and `add_weld_constraint` in Genesis 0.3.10.
- Start with `backend=gs.cpu` before switching to Metal.
